# 重要：在运行前将该文件改名为 config.yaml，并替换其中的占位符值

# 服务端配置
server:
  port: 8000          # 你希望的 8000 端口
  mode: "debug"       # 运行模式: debug 或 release

# AI 解析配置 
ai:
  api_key: "your_api_key_here"
  api_url: "https://api.openai.com/v1/chat/completions"
  model: "gpt-3.5-turbo"